import {build} from 'esbuild';
import {readFile,mkdir,writeFile} from 'node:fs/promises';
const result=await build({entryPoints:['scripts/standalone.tsx'],bundle:true,write:false,format:'iife',platform:'browser',target:'es2020',minify:true,define:{'process.env.NODE_ENV':'"production"'},jsx:'automatic',alias:{'@':process.cwd()}});
const css=(await readFile('app/globals.css','utf8')).replace(/^@import[^;]+;/gm,'').replace(/^@custom-variant[^;]+;/gm,'').replace(/^@theme inline \{[^\n]+\}/gm,'');
const appearance=await readFile('public/appearance.js','utf8');
const html=`<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover"><meta name="theme-color" content="#123736"><title>Mario - First Person Adventure</title><meta name="description" content="Explore a 3D Mushroom Kingdom, collect coins, stomp Goombas, and race to the flag in a first-person browser platformer."><link rel="icon" type="image/svg+xml" href="favicon.svg"><script id="appearance-init">${appearance.replaceAll('</script','<\\/script')}</script><style>${css}</style></head><body><div id="root"></div><script>${result.outputFiles[0].text.replaceAll('</script','<\\/script')}</script></body></html>`;
await mkdir('public',{recursive:true});await writeFile('public/mario.html',html);console.log(`Standalone HTML exported: ${Math.round(Buffer.byteLength(html)/1024)} KB, all scripts and styles embedded.`);
